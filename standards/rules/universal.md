---
# <!-- generated by tools/generate-rules.py -->
# Source: standards/common/UNIVERSAL.md
# Regenerate with: python tools/generate-rules.py --file UNIVERSAL.md
source: common/UNIVERSAL.md
---

# HyperI Universal Coding Rules

## Spelling Split

| Context | Dialect | Examples |
|---|---|---|
| Code identifiers | American English | `color`, `initialize`, `optimize`, `serialize` |
| Everything else (docs, comments, commits, chat) | Australian English | `colour`, `initialise`, `optimise`, `serialise` |

## Git Conventions

- Default branch: `main`
- Commit format: `<type>: <description>` — 50 chars max, lowercase, no period, imperative
- **Default type: `fix:`** — AI assistants exaggerate importance; if you think `feat:`, it's probably `fix:`
- Body: only for huge changes, 3 lines max. No emojis. No AI attribution.
- `git pull --rebase` before every push
- **Seek approval before any push** — show commit list and projected version bump
- Branch: `<type>/<issue>/<description>` or `<type>/<description>`

| Type | Bump | Type | Bump |
|---|---|---|---|
| `fix:` (DEFAULT) | PATCH | `feat:` (SPARINGLY) | MINOR |
| `perf:`, `sec:` | PATCH | `docs:`, `test:`, `chore:`, `ci:`, `refactor:`, `infra:`, `ops:`, `debt:`, `spike:`, `cleanup:` | None |

## Code Style

- Clarity over cleverness — break compound operations into clear steps
- Comments explain **WHY**, never WHAT — never number comments
- Helper functions FIRST, main function LAST
- No decorative separators (`# ====`, `# ────`)
- No TODOs/FIXMEs in production code
- Complete implementations only — no placeholders, no stubs

## File Headers

```
# Project:   <NAME>
# File:      <FILENAME>
# Purpose:   <One sentence>
# Language:  <LANGUAGE>
#
# License:   FSL-1.1-ALv2
# Copyright: (c) <YEAR> HYPERI PTY LIMITED
```

Never include: version numbers, change dates, author names, modification history.

## Error Handling

- Never expose: stack traces, DB schemas, file paths, raw exceptions
- Always log server-side: who, what, tracking ID, stack trace
- Specific exceptions over generic `except Exception: pass`
- Never log: passwords, tokens, API keys, credit cards, PII, private keys

## Configuration Cascade

**Priority (highest → lowest):** CLI args → ENV vars → `.env` → `settings.{env}.yaml` → `settings.yaml` → `defaults.yaml` → hard-coded

Always quote values in `.env` files.

## Security

- Validate ALL external input: type, range, format, sanitise
- Never commit secrets — use env vars or secret managers
- No `eval()`/`exec()` with external input
- Parameterised queries only (no f-string SQL)

## Testing

- 80% minimum coverage (90%+ for AI code)
- Structure: `tests/unit/`, `tests/integration/`, `tests/e2e/`
- **No mocks** — test against real dependencies (testcontainers, sandboxes, kind)
- If a test cannot run against the real thing, **skip it** — do not mock it

## Communication Style

- Direct, concise, technically accurate
- ❌ "Great question!", "I'd be happy to help!", "Amazing!", "Game-changing!"
- ✅ "This should help", "Performance is improved"

## Web Search Before Code (MANDATORY)

**NEVER use a dependency version from training data.** ALWAYS web search for current versions before adding any dependency or using any library API.

| ❌ Don't (Stale) | ✅ Do (Current) | Why |
|---|---|---|
| `psycopg2` / `psycopg2-binary` | `psycopg[binary]` (psycopg 3) | psycopg2 is legacy |
| `python-jose` | `joserfc` or `PyJWT` | unmaintained |
| `requests` | `httpx` | async, HTTP/2, modern |
| `datetime.utcnow()` | `datetime.now(UTC)` | deprecated Python 3.12 |
| `pkg_resources` | `importlib.metadata` | deprecated, slow |
| React class components | functional components + hooks | legacy |
| `moment.js` | `date-fns` or `Temporal` | maintenance mode |
| Webpack (new projects) | Vite | unless already using Webpack |

**When in doubt, SEARCH.**

## AI Code of Conduct

| ❌ NEVER | ✅ ALWAYS |
|---|---|
| Placeholders in committed code | Complete, working implementations |
| Claim "finished" without testing | Verify operations succeeded before reporting |
| Self-promote or use marketing language | Be concise and direct — skip pleasantries |
| Hardcoded example data ("John Doe") | Test code before claiming it works |

## Three-Iteration Rule

```
Iteration 1: Generate implementation
Iteration 2: Fix issues (tests, validation)
Iteration 3: Polish (formatting, docs)
STOP — Commit or revert
```

**Revert if:** 3+ iterations with no progress, code more complex than start, tests failing with unclear cause.

## Source of Truth

| Data | Source | NOT From |
|---|---|---|
| Version | `git describe --tags` or `VERSION` | STATE.md, agent memory |
| Tasks | `TODO.md` | STATE.md, agent memory |
| History | `git log` | STATE.md, agent memory |
| Changelog | `CHANGELOG.md` (semantic-release) | STATE.md, agent memory |
| Static context | `STATE.md` (symlinked as CLAUDE.md, CURSOR.md, etc.) | agent memory |
| Personal prefs | agent memory (if available) | STATE.md |

**STATE.md wins** over agent memory on any conflict. Do NOT duplicate STATE.md into agent memory.

## Licensing

- Default: **FSL-1.1-ALv2**
- Repos: always `--private` unless explicitly requested public
- No GPL/AGPL/SSPL dependencies permitted
- Never create custom LICENSE files — use the template

## Repository Naming

| Prefix | Scope |
|---|---|
| `hyperi-*` | Company-wide / product-agnostic |
| `dfe-*` | DFE product |
| `edge-*` | Edge Stream Hub |

- Lowercase, hyphen-separated — no underscores, no camelCase
- 1–3 words after prefix; avoid redundant suffixes (`-service`, `-app`, `-repo`)
- External forks: retain upstream name
- Cross-product scope → `hyperi-*`

## Temporary Files

- Dev/CI: `./.tmp/` (project-scoped, gitignored)
- Production: use language tempfile libraries with auto-cleanup
- Never hardcode `/tmp`, never use predictable names

## Agent-Specific Config

| Agent | Config Location | Notes |
|---|---|---|
| Claude Code | `~/.claude/projects/<hash>/memory/` | Local, per-developer, never committed. MEMORY.md capped at 200 lines. |
| Cursor IDE | `.cursor/rules/*.mdc` | Converted from `standards/rules/`. Notepads are per-user/ephemeral. |
| Gemini | `.gemini/` | Same principle: don't duplicate STATE.md into agent-local storage. |
